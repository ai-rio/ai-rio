/**
 * Component Translations - English
 *
 * Modular translation files for reusable UI components.
 * Each component gets its own namespace.
 *
 * Structure:
 * - Button: Button labels and variants
 * - Form: Form field labels, placeholders, validation messages
 * - Navigation: Navigation-related strings
 * - etc.
 *
 * @example
 * ```json
 * {
 *   "button": {
 *     "submit": "Submit",
 *     "cancel": "Cancel"
 *   },
 *   "form": {
 *     "required": "This field is required"
 *   }
 * }
 * ```
 */

export default {};
